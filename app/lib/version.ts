/**
 * The app version, shown in the sidebar footer and attached to support
 * diagnostics. One constant so a bug report can never name a version that
 * disagrees with what the merchant sees on screen.
 */
export const APP_VERSION = "1.0.0";
